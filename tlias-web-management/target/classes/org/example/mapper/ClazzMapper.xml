<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ClazzMapper">
    <select id="list" resultType="org.example.pojo.Clazz">
        select
            clazz.id, clazz.name name, clazz.room,
            clazz.begin_date, clazz.end_date, clazz.master_id,
            clazz.create_time, clazz.update_time, emp.name master_name
        from
            clazz left join emp
        on
            clazz.master_id = emp.id
        <where>
            <if test="name != null and name != ''">
                clazz.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and clazz.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by clazz.update_time desc
    </select>

    <!-- 根据 id 更新班级信息 -->
    <!-- set 标签和 where 标签一样，可以避免有多余的逗号。if标签：动态sql-->
    <update id="updateById">
        Update clazz
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="room != null and room != ''">
                room = #{room},
            </if>
            <if test="beginDate != null">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null">
                end_date = #{endDate},
            </if>
            <if test="masterId != null">
                master_id = #{masterId},
            </if>
            <if test="subject != null and subject != ''">
                subject = #{subject},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>