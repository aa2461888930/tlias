<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EmpMapper">
    <!-- resultType：查询返回的单条记录所封装的类型    -->
    <!-- 传递参数，依然跟注解格式一样，#{}。 #{}不能出现在引号大括号之内   -->
    <!-- 动态sql: <where>标签 生成where关键字 动态构建sql语句, 并去除多余的 or 或者 and-->
    <select id="list" resultType="org.example.pojo.Emp">
        select
            e.*, d.name deptName
        from
            emp e left join dept d
        on
            e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                e.name like concat('%', #{name}, '%')
            </if>
            <if test="gender != null">
                and e.gender=#{gender}
            </if>
            <if test="begin != null and end != null">
                and e.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by e.update_time desc
    </select>

    <delete id="deleteByIdList">
        delete from emp where id in
        <foreach collection="idList" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <resultMap id="empResultMap" type="org.example.pojo.Emp">
        <!-- 封装员工基本信息  property成员变量名 column数据库字段名-->
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="name" column="name" />
        <result property="gender" column="gender" />
        <result property="phone" column="phone" />
        <result property="job" column="job" />
        <result property="salary" column="salary" />
        <result property="image" column="image" />
        <result property="entryDate" column="entry_date" />
        <result property="deptId" column="dept_id" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />

        <!-- 封装集合collection：封装员工工作经历信息 注意是ofType -->
        <collection property="exprList" ofType="org.example.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="empId" column="ee_emp_id" />
            <result property="begin" column="ee_begin" />
            <result property="end" column="ee_end" />
            <result property="company" column="ee_company" />
            <result property="job" column="ee_job" />
        </collection>
    </resultMap>

    <!-- 自定义映射规则resultMap， 将数据库字段 封装到 类成员变量 。数据库结果可多条：更加通用-->
    <select id="getEmpById" resultMap="empResultMap">
        select
            e.*,
            ee.id ee_id,
            ee.emp_id ee_emp_id,
            ee.begin ee_begin,
            ee.end ee_end,
            ee.company ee_company,
            ee.job ee_job
        from
            emp e left join emp_expr ee
        on
            e.id = ee.emp_id
        where
            e.id = #{id}
    </select>

    <!-- 根据 id 更新员工信息 -->
    <!-- set 标签和 where 标签一样，可以避免有多余的逗号。if标签：动态sql-->
    <update id="updateById" >
        UPDATE emp
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="job != null and job != ''">
                job = #{job},
            </if>
            <if test="salary != null">
                salary = #{salary},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="entryDate != null">
                entry_date = #{entryDate},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <!-- 更新时间通常建议强制更新，或者也可以加上 if 判断 -->
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 统计员工职位人数 -->
    <!-- resultType：查询返回的单条记录所封装的类型，返回字典类型。 主要用于<select>标签。   -->
    <select id="countEmpJobData" resultType="java.util.Map">
        select
            (case when job=1 then '班主任'
                  when job=2 then '讲师'
                  when job=3 then '学工主管'
                  when job=4 then '教研主管'
                  when job=5 then '咨询师'
                  else '其他' end) pos,
            count(*) num
        from
            emp
        group by
            job
        order by
            num;
    </select>

    <!-- 返回类型：字典Map -->
    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if(gender=1, '男性员工', '女性员工') name,
            count(*) value
        from
            emp
        group by
            gender

    </select>
</mapper>